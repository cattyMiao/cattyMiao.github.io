# frozen_string_literal: true

source "https://rubygems.org"

# 使用 GitHub Pages 官方 gem，它会自动锁定兼容的版本
gem "github-pages", group: :jekyll_plugins

# 这是加载远程主题必需的插件
gem "jekyll-remote-theme"

# 如果你之后需要其他插件，可以加在这里，但先保证基础功能
